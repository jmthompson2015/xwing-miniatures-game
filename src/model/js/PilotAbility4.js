/*
 * Provides pilot abilities for the End Phase.
 */
"use strict";

define(function()
{
   var PilotAbility4 = {};

   PilotAbility4.toString = function()
   {
      return "model/js/PilotAbility4";
   };

   if (Object.freeze)
   {
      Object.freeze(PilotAbility4);
   }

   return PilotAbility4;
});
